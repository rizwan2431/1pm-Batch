Playbook to create new user in the node machine



---
- hosts: appservers
  become: true
  become_user: root
  tasks:
  - name: Create User
    user: name=advitha password=advitha groups=ansibleadm shell=/bin/bash

